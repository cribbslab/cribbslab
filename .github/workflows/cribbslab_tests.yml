name: cribbslab

on: [push, pull_request]

jobs:
  build:
    name: Ex1 (${{ matrix.python-version }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        # macos-13 is Intel (osx-64). We deliberately avoid macos-latest, which
        # is Apple Silicon (osx-arm64) where several bioconda/conda-forge
        # packages used here (e.g. multiqc's deps pyaml-env / lzstring) have no
        # build, causing the environment solve to fail.
        os: ["ubuntu-latest", "macos-13"]
        python-version: ["3.10", "3.11"]

    defaults:
      run:
        shell: bash -l {0}

    steps:
      - uses: actions/checkout@v2
      - name: Cache conda
        uses: actions/cache@v3
        env:
          # Increase this value to reset cache if conda/environments/cribbslab.yml has not changed
          CACHE_NUMBER: 0
        with:
          path: ~/conda_pkgs_dir
          key:
            ${{ runner.os }}-conda-${{ env.CACHE_NUMBER }}-${{
            hashFiles('conda/environments/cribbslab.yml') }}
      - uses: conda-incubator/setup-miniconda@v3
        with:
          mamba-version: "*"
          python-version: ${{ matrix.python-version }}
          channels: conda-forge, bioconda, defaults
          channel-priority: true
          activate-environment: cgat-core
          environment-file: conda/environments/cribbslab.yml
      - name: Show conda
        run: |
          conda info
          conda list
      - name: Test
        run: |
          python setup.py install
          pycodestyle --ignore=E501 tests
          python tests/template_pipeline.py --local make all
          nosetests -v tests/test_import.py
          nosetests -v tests/test_style.py
          nosetests -v tests/test_pipeline_control.py
